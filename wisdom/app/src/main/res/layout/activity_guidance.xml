<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.cnst.wisdom.ui.activity.Guidance">

    <include
        android:id="@+id/in_head_title"
        layout="@layout/head_back"
        android:layout_width="match_parent"
        android:layout_height="@dimen/headHeight" />
    <com.cnst.wisdom.ui.widget.ExpandTabView
        android:layout_below="@+id/in_head_title"
        android:id="@+id/expand_tab"
        android:layout_width="match_parent"
        android:layout_height="50dp"/>
    <ListView
        android:id="@+id/listView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:cacheColorHint="@color/no_color"
        android:divider="#dcdcdc"
        android:dividerHeight="1px"
        android:paddingLeft="4dp"
        android:paddingRight="4dp"
        android:layout_below="@+id/expand_tab">
    </ListView>
</RelativeLayout>
